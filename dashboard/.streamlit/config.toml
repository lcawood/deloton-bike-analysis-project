[theme]
# primary accent colour for side bar (dark gray).
primaryColor = '#343434'
# background colour for main content area (dark_gray).
backgroundColor = '#343434'
# secondary background colour (forest green).
secondaryBackgroundColor = '#477346'
# font colour (white).
textColor="#ffffff"
font="sans serif"
